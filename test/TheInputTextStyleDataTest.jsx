/**
 * Test for TheInputTextStyleData.
 * Runs with mocha.
 */
'use strict'

import TheInputTextStyleData from '../lib/styleData/TheInputTextStyleData'
import React from 'react'
import { ok, equal } from 'assert'
import { render } from 'the-script-test'

describe('the-input-text-style-data', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
  })
})

/* global describe, before, after, it */
